DATABASE_URL=postgresql://postgres:postgres@localhost:6432/profitability_tracker
REDIS_HOST=localhost
REDIS_PORT=6679
API_PORT=4101
WEB_PORT=4100
# openssl rand -base64 32
JWT_SECRET=change-me-in-production
JWT_EXPIRES_IN=7d
SMTP_HOST=
SMTP_PORT=587
SMTP_USER=
SMTP_PASS=
SMTP_FROM=
SUPER_ADMIN_EMAIL=admin@example.com
SUPER_ADMIN_PASSWORD=ChangeMe123!
SUPER_ADMIN_NAME=Super Admin
CORS_ORIGIN=http://localhost:4100
# Optional. Dev Vite proxies /api and /socket.io here. Not inlined into the production bundle.
# VITE_API_PROXY_TARGET=http://localhost:4101
POSTGRES_USER=postgres
POSTGRES_PASSWORD=postgres
POSTGRES_DB=profitability_tracker
POSTGRES_PORT=6432
# Optional. Used when pg_dump is not installed locally (common on Windows dev).
POSTGRES_CONTAINER=profitability-tracker-postgres
# Optional. Path to pg_dump when it is not on PATH.
# PG_DUMP_PATH=C:\Program Files\PostgreSQL\16\bin\pg_dump.exe
REDIS_PUBLISH_PORT=6679
