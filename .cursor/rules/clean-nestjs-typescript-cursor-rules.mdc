---
globs: apps/api/**/*
alwaysApply: false
---
You are a senior TypeScript programmer with experience in the NestJS framework and a preference for clean programming and design patterns.

Generate code, corrections, and refactorings that comply with the basic principles and nomenclature.

## TypeScript General Guidelines

### Basic Principles

- Use English for all code and documentation.
- Always declare the type of each variable and function (parameters and return value).
  - Avoid using any.
  - Create necessary types.
- Use JSDoc to document public classes and methods.
- Don't leave blank lines within a function.
- One export per file except for index.ts files.

### Nomenclature

- Use PascalCase for classes.
- Use camelCase for variables, functions, and methods.
- Use kebab-case for file and directory names.
- Use UPPERCASE for environment variables.
  - Avoid magic numbers and define constants.
- Start each function with a verb.
- Use verbs for boolean variables. Example: isLoading, hasError, canDelete, etc.
- Use complete words instead of abbreviations and correct spelling.
  - Except for standard abbreviations like API, URL, etc.
  - Except for well-known abbreviations:
    - i, j for loops
    - err for errors
    - ctx for contexts
    - req, res, next for middleware function parameters

### Functions

- In this context, what is understood as a function will also apply to a method.
- Write short functions with a single purpose. Less than 20 instructions.
- Name functions with a verb and something else.
  - If it returns a boolean, use isX or hasX, canX, etc.
  - If it doesn't return anything, use executeX or saveX, etc.
- Avoid nesting blocks by:
  - Early checks and returns.
  - Extraction to utility functions.
- Use higher-order functions (map, filter, reduce, etc.) to avoid function nesting.
  - Use arrow functions for simple functions (less than 3 instructions).
  - Use named functions for non-simple functions.
- Use default parameter values instead of checking for null or undefined.
- Reduce function parameters using RO-RO
  - Use an object to pass multiple parameters.
  - Use an object to return results.
  - Declare necessary types for input arguments and output.
- Use a single level of abstraction.

### Data

- Don't abuse primitive types and encapsulate data in composite types.
- Avoid data validations in functions and use classes with internal validation.
- Prefer immutability for data.
  - Use readonly for data that doesn't change.
  - Use as const for literals that don't change.

### Classes

- Follow SOLID principles.
- Prefer composition over inheritance.
- Declare interfaces to define contracts.
- Write small classes with a single purpose.
  - Less than 200 instructions.
  - Less than 10 public methods.
  - Less than 10 properties.

### Exceptions

- Use exceptions to handle errors you don't expect.
- If you catch an exception, it should be to:
  - Fix an expected problem.
  - Add context.
  - Otherwise, use a global handler.
- Follow RFC 9457 error response standards

### Testing

- Follow the Arrange-Act-Assert convention for tests.
- Name test variables clearly.
  - Follow the convention: inputX, mockX, actualX, expectedX, etc.
- Write unit tests for each public function.
  - Use test doubles to simulate dependencies.
    - Except for third-party dependencies that are not expensive to execute.
- Write acceptance tests for each module.
  - Follow the Given-When-Then convention.


  ## Specific to NestJS

  ### Basic Principles
  
  - Use modular architecture.
  - Use monorepo mode with client, dashboard, employee and partner.
  - Encapsulate the API in modules. (Naming convention: should be plural)
    - One module per main domain/route. (Naming convention examples: check-ins.module.ts, subscriptions.module.ts)
    - One controller for its route. (Naming convention examples: check-ins.controller.ts, subscriptions.controller.ts)
      - And other controllers for secondary routes.
    - A dto folder with data types. (Naming convention examples: create-check-in.dto.ts, find-all-check-ins-response.dto.ts)
      - DTOs validated with class-validator and class-transformer for inputs.
      - Declare simple DTOs (without validators) types for outputs with swagger decorators so that codegen can generate types correctly.
    - A services module with business logic and persistence. (Naming convention examples: check-ins.service.ts, subscriptions.service.ts)
      - No need for entities, use Prisma directly through prismaService which is a global module
      - One service per entity.
    - A swagger file containing all decorators that will be exported and imported in controller (Naming convention examples: check-ins.swagger.ts, subscriptions.swagger.ts)
  
  - Use _shared directory inside apps to put common parts of code across the monorepo apps
    - constants folder for storing commonly shared constants (Eg. reaction-types.constant.ts)
    - decorators folder for common decorators (Eg. auth.decorator.ts, is-nano-id.decorator.ts)
    - dto folder for common DTOs (Eg. location.dto.ts, audit-user.dto.ts)
    - interceptors for transforming request
        - transform-res.interceptor.ts to transform response in the following way:
            - Single resource: { data: any{} }
            - Paginated resource: { data: any[], meta: { total: 1 } | any{} }
            - Multiple resource: { data: any[] }
            Note: if data and meta in object is returned by the service/controller then meta is automatically set, else whatever is returned by service or controller is encapsulated inside data
    - pipes for common pipes (Eg. parse-nano-id.pipe.ts, pagination.pipe.ts)
    - queues for holding common jobs
    - sentry instrument file
    - common swagger setup for all 4 monorepo apps
    - types for holding common shared types
    - utils for holding common helper and utility functions
    - validators for holding common validators (Eg. new-password.validator.ts)

  - Use nest libraries for common modules shared across the monorepo apps (such as for prisma, nodemailer, etc.)
  
  - Core module functionalities:
    - Global filters for exception handling.
    - Global middlewares for request management.
    - Guards for permission management.

### Testing

- Use the standard Jest framework for testing.
- Write tests for each controller and service.
- Write end to end tests for each api module.
- Add a admin/test method to each controller as a smoke test.
 